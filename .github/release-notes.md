## 更新内容

- 日志区恢复为 v0.2.4 的虚拟化 `ListBox` 渲染，恢复熟悉的行距、滚动和视觉密度。
- 移除 `FlowDocument` 日志渲染，解决长时间运行后原生内存增长、界面无响应以及选词和光标行为异常的问题。
- 恢复日志整行多选、`Ctrl+A`、`Ctrl+C`、右键复制选中日志以及回车恢复自动滚动。
- 应用每次启动约 3 秒后主动检查一次更新，不再受上次检查时间缓存限制。

## 说明

- 日志区以整行为选择和复制单位，不再提供字符级选词和复制。
- 便携版可通过“帮助 → 检查更新”下载安装本版本。
- 多机协作电脑建议统一升级至相同版本。
