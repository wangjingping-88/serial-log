<Window x:Class="SerialLog.App.Views.DeleteConfirmationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="确认删除"
        Icon="/SerialLog.App;component/Assets/AppIcon.ico"
        Width="460" MinHeight="220" SizeToContent="Height"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False" AllowsTransparency="False"
        WindowStyle="SingleBorderWindow" Background="#EEF2F6">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="TitleText" FontSize="18" FontWeight="SemiBold" TextWrapping="Wrap" />
        <ScrollViewer Grid.Row="1" Margin="0,8,0,24" MaxHeight="260"
                      VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <TextBlock x:Name="DetailText" Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap" />
        </ScrollViewer>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CancelButton" Content="取消" MinWidth="72" Margin="0,0,8,0"
                    Style="{StaticResource CompactButton}" IsCancel="True" IsDefault="True" />
            <Button x:Name="DeleteButton" Content="删除" MinWidth="72" Margin="0"
                    Style="{StaticResource CompactDangerButton}" Click="DeleteButton_Click" />
        </StackPanel>
    </Grid>
</Window>
